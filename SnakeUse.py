#load_model(filepath)
import tensorflow as tf
from tensorflow import keras
import numpy as np

class SnakeUse:

    def init(self,path):
        self.path=""
        self.model = load_model(path)

    #load model


    def Interpretor():
        return[]
